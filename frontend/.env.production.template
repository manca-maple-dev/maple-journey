# Production environment — copy to .env before building
# Replace values with your real production URLs

REACT_APP_BACKEND_URL=https://api.maplejourney.ca
ENABLE_HEALTH_CHECK=true

# Stripe publishable key is not required for the current redirect-based checkout flow.
# If you later move to Stripe Elements, add REACT_APP_STRIPE_PUBLISHABLE_KEY in Vercel.
